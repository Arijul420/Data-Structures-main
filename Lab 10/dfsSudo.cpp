push() starting node makeVisited() and print()
while(!isEmpty())
    p=top()
    if(hasValidAdjNodeForP())
        push(one valid adj node)
        makeVisited()
        print()
    else
        pop()
